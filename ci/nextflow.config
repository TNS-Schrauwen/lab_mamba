params.container = null
params.command = null

docker.enabled = true
// Run as the same UID/GID as the Nextflow step user – this surfaces permission issues early.
docker.runOptions = '-u $(id -u):$(id -g)'

process {
    executor      = 'local'
    errorStrategy = 'terminate'
    maxRetries    = 1
    maxForks      = 1
}

