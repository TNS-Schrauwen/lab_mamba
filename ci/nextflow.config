params.container = null
params.command = null

docker.enabled = true
docker.runOptions = '-u $(id -u):$(id -g)'
workDir = '/tmp/nextflow-work'

process {
    executor      = 'local'
    errorStrategy = 'terminate'
    maxRetries    = 1
    maxForks      = 1
}

